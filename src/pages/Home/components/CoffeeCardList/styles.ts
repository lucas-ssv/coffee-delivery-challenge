import styled from "styled-components";

export const CoffeeCardContainer = styled.ul`
  list-style: none;
  margin-top: 3.375rem;

  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 2rem;
`